{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "parley",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "build:bin": "bun --filter '@parley/cli' run build:bin && bun --filter '@parley/api' run build:bin",
    "install:bin": "bun run ./scripts/install-bins.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@effect/language-service": "^0.86.1",
    "@types/bun": "latest",
    "comptime.ts": "^0.8.1",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@effect/experimental": "^0.60.0",
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.89.0",
    "effect": "^3.21.2"
  }
}
